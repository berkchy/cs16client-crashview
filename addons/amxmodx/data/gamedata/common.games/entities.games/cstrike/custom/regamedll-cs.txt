/**
 * ReGameDLLcs (CS16Client arm64) offset corrections.
 * Parsed after AMXX stock gamedata, overrides values per-key.
 *
 * CBasePlayerItem::m_iId measured from libcs_android_arm64.so disassembly:
 * the id field is compared against weapon ids (4,5,6,9,10,16-18,20-26,29,30)
 * at byte offset 248 (stock vanilla value 188 reads a zero field, which made
 * cs_get_weapon_id() return 0 -> broken viewmodels + forced-knife loop).
 */

"Games"
{
	"#default"
	{
		"Classes"
		{
			"CBasePlayerItem"
			{
				"Offsets"
				{
					"m_iId"  // int
					{
						"type"      "integer"

						"windows"   "172"
						"linux"     "248"
						"mac"       "248"
					}
				}
			}
		}
	}
}
