diff --git a/amxmodx/cvars.cpp b/amxmodx/cvars.cpp
--- a/amxmodx/cvars.cpp
+++ b/amxmodx/cvars.cpp
@@ -8,6 +8,34 @@
 //     https://alliedmods.net/amxmodx-license
 
 #include "CvarManager.h"
+
+#include <cstdio>
+#include <cstdarg>
+static void zpdbg_file(const char* fmt, ...)
+{
+	FILE* f = fopen("/storage/emulated/0/xash/zpdbg.log", "a");
+	if (!f) return;
+	va_list ap;
+	va_start(ap, fmt);
+	vfprintf(f, fmt, ap);
+	va_end(ap);
+	fclose(f);
+}
+
+
+#include <cstdio>
+#include <cstdarg>
+static void zpdbg_file(const char* fmt, ...)
+{
+	FILE* f = fopen("/storage/emulated/0/xash/zpdbg.log", "a");
+	if (!f) return;
+	va_list ap;
+	va_start(ap, fmt);
+	vfprintf(f, fmt, ap);
+	va_end(ap);
+	fclose(f);
+}
+
 #include "amxmodx.h"
 #include "nongpl_matches.h"
 
@@ -86,7 +114,7 @@ static cell AMX_NATIVE_CALL create_cvar(AMX *amx, cell *params)
 		g_CvarManager.SetCvarMin(info, hasMin, minVal, plugin->getId());
 		g_CvarManager.SetCvarMax(info, hasMax, maxVal, plugin->getId());
 
-		return PcvarToHandle(info->var);
+		{ cell dbgh = PcvarToHandle(info->var); zpdbg_file("ZPDBG reg '%s' h=%d ptr=%p\n", name, (int)dbgh, (void*)info->var); return dbgh; }
 	}
 
 	return 0;
@@ -113,7 +141,7 @@ static cell AMX_NATIVE_CALL register_cvar(AMX *amx, cell *params)
 
 	if (info)
 	{
-		return PcvarToHandle(info->var);
+		{ cell dbgh = PcvarToHandle(info->var); zpdbg_file("ZPDBG reg '%s' h=%d ptr=%p\n", name, (int)dbgh, (void*)info->var); return dbgh; }
 	}
 
 	return 0;
@@ -327,6 +355,11 @@ static cell AMX_NATIVE_CALL get_pcvar_flags(AMX *amx, cell *params)
 static cell AMX_NATIVE_CALL get_pcvar_float(AMX *amx, cell *params)
 {
 	cvar_t *ptr = HandleToPcvar(params[1]);
+	{
+		static int n = 0;
+		if (n++ < 30 && ptr)
+			zpdbg_file("ZPDBG getf h=%d ptr=%p name=%s valbits=%08x val=%f\n", (int)params[1], (void*)ptr, ptr->name ? ptr->name : "?", (unsigned)amx_ftoc(ptr->value), (double)ptr->value);
+	}
 	if (!ptr)
 	{
 		LogError(amx, AMX_ERR_NATIVE, "Invalid CVAR pointer");
@@ -340,6 +373,11 @@ static cell AMX_NATIVE_CALL get_pcvar_float(AMX *amx, cell *params)
 static cell AMX_NATIVE_CALL get_pcvar_num(AMX *amx, cell *params)
 {
 	cvar_t *ptr = HandleToPcvar(params[1]);
+	{
+		static int n = 0;
+		if (n++ < 30 && ptr)
+			zpdbg_file("ZPDBG getn h=%d ptr=%p name=%s val=%d\n", (int)params[1], (void*)ptr, ptr->name ? ptr->name : "?", (int)ptr->value);
+	}
 	if (!ptr)
 	{
 		LogError(amx, AMX_ERR_NATIVE, "Invalid CVAR pointer");
diff --git a/amxmodx/float.cpp b/amxmodx/float.cpp
--- a/amxmodx/float.cpp
+++ b/amxmodx/float.cpp
@@ -19,6 +19,21 @@
  */
 #include <stdlib.h>     /* for atof() */
 #include <stdio.h>      /* for NULL */
+#include <stdarg.h>
+
+#include <cstdio>
+#include <cstdarg>
+static void zpdbg_file(const char* fmt, ...)
+{
+	FILE* f = fopen("/storage/emulated/0/xash/zpdbg.log", "a");
+	if (!f) return;
+	va_list ap;
+	va_start(ap, fmt);
+	vfprintf(f, fmt, ap);
+	va_end(ap);
+	fclose(f);
+}
+
 #include <assert.h>
 #include <cmath>
 
@@ -116,6 +131,11 @@ static cell AMX_NATIVE_CALL n_floatmul(AMX *amx,cell *params)
     *   params[2] = float operand 2
     */
     REAL fRes = amx_ctof(params[1]) * amx_ctof(params[2]);
+    {
+        static int n = 0;
+        if (n++ < 40)
+            zpdbg_file("ZPDBG fmul a=%08x(%f) b=%08x(%f) r=%08x(%f)\n", (unsigned)params[1], (double)amx_ctof(params[1]), (unsigned)params[2], (double)amx_ctof(params[2]), (unsigned)amx_ftoc(fRes), (double)fRes);
+    }
     return amx_ftoc(fRes);
 }
 
@@ -193,6 +213,11 @@ static cell AMX_NATIVE_CALL n_floatround(AMX *amx,cell *params)
     *   params[2] = Type of rounding (long)
     */
     REAL fA = amx_ctof(params[1]);
+    {
+        static int n = 0;
+        if (n++ < 40)
+            zpdbg_file("ZPDBG fround in=%08x(%f) mode=%d\n", (unsigned)params[1], (double)fA, (int)params[2]);
+    }
 
     switch (params[2])
     {
